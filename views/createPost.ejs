<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog - André Carvalho</title>
    <link rel="stylesheet" href="/styles/styles.css"/>
</head>
<body>
    <main>
        <%- include("partials/header.ejs") %>

        <form action=<%= post ? `/edit/${post.id}` : "/add" %> method="post">
            <div class="form title">
                <label for="title">Título</label>
                <input type="text" id="title" name="title" required value="<%= post ? post.titulo : "" %>">
            </div>

            <div class="form prev">
                <label for="prev">Prévia</label>
                <textarea rows="3" cols="60" id="prev" name="prev"><%= post ? post.previa : "" %></textarea>
            </div>

            <div class="form texxt">
                <label for="texxt">Texto</label>
                <textarea rows="30" cols="60" id="texxt" name="texxt"><%= post ? post.conteudo : "" %></textarea>
            </div>
            <div class="form"><button type="submit">Postar</button></div>  
        </form>

        <%- include("partials/footer.ejs") %>
    </main>
</body>
</html>